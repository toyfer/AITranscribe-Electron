## 変更概要

<!-- この PR で何を変更したか、1-2 文で -->

## 関連 Issue

<!-- Fixes #NNN または Refs #NNN -->

## 手動動作確認結果

<!-- マージ前に必ず以下の動作確認を行ってください -->

- [ ] `npm start` でアプリが起動する
- [ ] 「ファイル選択」ボタンをクリック → ダイアログが開く
- [ ] 音声ファイルを選択 → ファイル名が表示される
- [ ] 「スタート」ボタンをクリック → 文字起こしが開始される
- [ ] 進捗バーが更新される
- [ ] ログに出力が表示される
- [ ] 完了通知が出る
- [ ] （要約機能がある場合）CSV 選択 → 要約実行 → docx 出力

## 削除されたファイル / 削除行数

<!-- 削除したファイルや大量削除がある場合は明記 -->

## `git diff --stat` の要約

```
<!-- 例: 3 files changed, 120 insertions(+), 45 deletions(-) -->
```

## チェックリスト

- [ ] `node --check src/renderer.js` が通る
- [ ] `class UIController` と `new UIController()` が存在する
- [ ] renderer.js が 50 行以上ある
- [ ] CI (syntax-check) が通る
