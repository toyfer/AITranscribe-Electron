# Faster-Whisper stack for Python Embeddable (air-gapped runtime)
# Do NOT use git+https master tarballs in CI or production builds.
#
# Online machine (Embeddable python.exe):
#   .\python.exe -m pip install -r requirements-whisper.txt
# Then copy the entire src/Whisper folder (with site-packages) to the air-gapped PC.
#
# Models (UI v2.4.0):
#   small → Systran/faster-whisper-small          (速度重視)
#   turbo → deepdml/faster-whisper-large-v3-turbo-ct2  (精度重視・デフォルト)
# Runtime must not require network access.

faster-whisper==1.2.1

# Notes (faster-whisper 1.2.1):
# - ctranslate2>=4.0,<5
# - huggingface-hub>=0.21
# - tokenizers>=0.13,<1
# - onnxruntime>=1.14,<2  (VAD)
# - av>=11
# CPU-only air-gap: device=cpu / compute_type=int8
# Optional: AITRANSCRIBE_DEVICE / AITRANSCRIBE_COMPUTE
